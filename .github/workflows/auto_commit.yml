on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Git
        run: |
          git config --global user.email "kwaksh2319@gmail.com"
          git config --global user.name "kwaksh2319"

      - name: Create commit
        run: |
          echo "$(date -u)" >> daily_commit.txt
          git add daily_commit.txt
          git commit -m "Daily commit: $(date -u)"

      - name: Debug info
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          echo "GH_PAT value: $GH_PAT"

      - name: Push changes
        run: |
          git remote set-url origin https://x-access-token:${{ secrets.GH_PAT }}@github.com/kwaksh2319/example.git
          git push origin HEAD:main
