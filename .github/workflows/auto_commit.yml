on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Git
        run: |
          git config --global user.email "kwaksh2319@gmail.com"
          git config --global user.name "kwaksh2319"

      - name: Create commit
        run: |
          echo "$(date -u)" >> daily_commit.txt
          git add daily_commit.txt
          git commit -m "Daily commit: $(date -u)"

      
      - name: Push changes
        run: |
          git remote set-url origin https://x-access-token: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@github.com/kwaksh2319/example.git
          git push origin HEAD:main
